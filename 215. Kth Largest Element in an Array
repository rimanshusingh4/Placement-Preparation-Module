class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {
        int ans =0;
        int n = nums.size();
        sort(nums.begin(),nums.end());
       // for(int i =nums.size()-1;i>=n-k;i--){
        ans = nums[n-k];
        return ans;
      //  }
    }
};
