class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int ans;
        vector<int> arr;
        for(int i=0;i<nums.size();i++)
{
            ans = nums[i] ^ nums[i+1];
        }
        return ans;
        
    }
};
